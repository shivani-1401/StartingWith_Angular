export class Employee {
    constructor(public id: number, public firstname: string, public lastname: string, public address: string, public email: string, public password: string, public gender: string, public date_of_birth: string) {
    }

}
